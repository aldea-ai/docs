---
title: Raw HTTP requests
description: Call the SubQ API directly without the Deepgram SDK
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Raw HTTP requests

<Tabs items={['Python (httpx)', 'Node.js (fetch)', 'Go (net/http)', 'Rust (reqwest)']}>
<Tab value="Python (httpx)">

Call the SubQ API directly using httpx without the Deepgram SDK.

</Tab>
<Tab value="Node.js (fetch)">

Call the SubQ API directly using fetch without the Deepgram SDK.

</Tab>
<Tab value="Go (net/http)">

Call the SubQ API directly using net/http without the Deepgram SDK.

</Tab>
<Tab value="Rust (reqwest)">

Call the SubQ API directly using reqwest without the Deepgram SDK.

</Tab>
</Tabs>
