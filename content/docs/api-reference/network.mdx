---
title: network
---

> This page is **auto-generated** from `/headers/network.h`. Edit doc comments in the header and rebuild to update.

## Functions
- [`net_delete`](#net-delete)
- [`net_get`](#net-get)
- [`net_post`](#net-post)
- [`net_put`](#net-put)
- [`net_set_retries`](#net-set-retries)
- [`net_set_timeout`](#net-set-timeout)

<a id="net-delete"></a>
### `net_delete`
Performs a DELETE request.

**Signature**
```c
int net_delete(const char* path);
```

**Parameters**

| Name | Description |
|------|-------------|
| `path` | Path portion. |

**Returns**

0 on success; negative on error.

<a id="net-get"></a>
### `net_get`
Performs a GET request and returns raw JSON.

**Signature**
```c
int net_get(const char* path, const char* query, char* json_out, int json_out_size);
```

**Parameters**

| Name | Description |
|------|-------------|
| `path` | Path portion (e.g., "/users"). |
| `query` | Optional query string (nullable). |
| `json_out` | Buffer to receive body. |
| `json_out_size` | Size of buffer. |

**Returns**

0 on success; negative on network or HTTP error.

<a id="net-post"></a>
### `net_post`
Performs a POST request with JSON body.

**Signature**
```c
int net_post(const char* path, const char* json_body, char* json_out, int json_out_size);
```

**Parameters**

| Name | Description |
|------|-------------|
| `path` | Path portion. |
| `json_body` | JSON string to send. |
| `json_out` | Buffer to receive response body. |
| `json_out_size` | Size of buffer. |

**Returns**

0 on success; negative on error.

<a id="net-put"></a>
### `net_put`
Performs a PUT request with JSON body.

**Signature**
```c
int net_put(const char* path, const char* json_body, char* json_out, int json_out_size);
```

**Parameters**

| Name | Description |
|------|-------------|
| `path` | Path portion. |
| `json_body` | JSON string to send. |
| `json_out` | Buffer to receive response body. |
| `json_out_size` | Size of buffer. |

**Returns**

0 on success; negative on error.

<a id="net-set-retries"></a>
### `net_set_retries`
Sets retry attempts for idempotent requests.

**Signature**
```c
int net_set_retries(int retries);
```

**Parameters**

| Name | Description |
|------|-------------|
| `retries` | Number of retries (>=0). |

**Returns**

0 on success; -1 invalid value.

<a id="net-set-timeout"></a>
### `net_set_timeout`
Sets default request timeout (milliseconds).

/
/**

**Signature**
```c
int net_set_timeout(int timeout_ms);
```

**Parameters**

| Name | Description |
|------|-------------|
| `timeout_ms` | Timeout value in ms. |

**Returns**

0 on success; -1 invalid value.

